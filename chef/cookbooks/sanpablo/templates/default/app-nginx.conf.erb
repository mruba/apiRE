log_format main '$remote_addr - $remote_user [$time_local] '
                '"$request" $status  $body_bytes_sent "$http_referer" '
                '"$http_user_agent" "$http_x_forwarded_for" "$upstream_response_time" "$upstream_addr" "$gzip_ratio"';
#include <%= node.deploy.home_dir %>/app/*/shared/config/nginx.conf;
include <%= node.deploy.home_dir %>/app/*/current/config/nginx.conf;
